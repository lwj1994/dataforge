/// The build_runner entry point for Dataforge.
///
/// Applications normally add `dataforge` to `dev_dependencies`. This library
/// exposes the builder for custom build pipelines and tests.
library dataforge;

export 'builder.dart' show dataforgeBuilder;
